// Logging is deliberately configured separately; do not overwrite the user's setting.
// No sv_cheats changes, key rebinding, map loading, saves, or autoexec changes here.
script_execute generative_glados/probe
